# 進捗

## 完了済み

- フェーズ1: 戦闘エンジン（六角グリッド・0.1秒の決定論シミュレーション・効果システム・4種の属性デバフ）、
  zod で検証するデータ定義、6種のシミュレーション、検証用画面
- フェーズ1.5:
  - UI整備（文字16px以上・タップ領域44px以上・CSS変数で一元管理、キャラ一覧の名前重複の修正、
    下部シート方式のポップアップ、画面下部の固定バー）
  - 加護キャリアの廃止 → チーム効果（TeamEffect）に置き換え
  - `initialAttackDelay`（攻撃の初期遅延）と `simultaneousWipe`（同時全滅の扱い）を設定値化
  - `CLAUDE.md` と `docs/` を作成
  - 加護の一覧表示（名前・1行の説明・レア度・［説明］）と、詳細シートの「チーム全体」項目
  - チーム効果の割合（%）対応（`statMod` に属性・役割・神話圏のフィルタを追加）
  - 仮タイトル「Divine Pawns」を反映
  - 装備スロット数を★の数に連動（`equipmentSlots(star)`）
  - 準備画面を「上＝盤面／下＝タブ付き操作バー（編成・加護・操作）」の2層に再構成

- フェーズ2:
  - A1 ポップアップを本当のスタックに（全階層を同時描画・背面は操作不可）
  - A2 キャラを1体ずつのカードで表示（◀▶・「3 / 8」表示）
  - A3 表示中キャラの盤面ハイライト（ピンク #ff7ae0）
  - B1〜B6 ラン進行（`src/game/`）：章ステートマシン・リザルト兼ショップ・二択イベント・
    ライフ・枠拡張報酬・オートセーブ。UI は「ラン」タブ

- フェーズ2.1:
  - 戦闘開始時の全リセット（`Battle.resetAll()` / `resetUnit()`）
  - 1章1戦目（E1）を大幅に弱体化し、8体×14マス×2遭遇＝224戦すべて勝利
  - キャラカードを4行に圧縮（スキル・装備は［▼ 装備］で開く）。縦の予算は `src/ui/layout.ts`
  - メイン画面を `screenMode` で切り替え（リザルト・ショップ・イベント・進行演出をメイン画面へ）

- フェーズ2.2:
  - 装備を20種に拡充（★1〜★3・属性特化つき）、加護を11種に拡充（4属性＋汎用）
  - スキルを「初期2つ＋合成時に3択」に変更（`CharacterDef.skills` / `initialSkills`、`mergeChars` / `chooseSkill`）
  - ショップのリロールを 1c → 2c → 3c…（ショップごとにリセット）
  - イベントの見通し表示と結果表示、リザルトと進行演出の1画面統合
  - 戦闘中以外はタブ操作を解禁、ラン中は所持品のみ表示、★の直接変更を廃止、下部バーの高さを固定

- フェーズ2.3:
  - 同キャラ2体所持を廃止（再獲得＝★+1、★3は出現候補から除外）。合成UIも撤去
  - 下部バーを 56vh → 292px 固定に縮小し、盤面を拡大（667px画面で 236px → 317px）
  - `RunConfig.chapterScale` を導入し、少数精鋭／多キャラの両ビルドが通せるよう調整
  - 装備はポップアップに統一（トグル廃止）、★アップ時のスキル3択を即時＋キュー表示
  - 「配置」ボタンを廃止し、盤面タップで1タップ移動／1タップ入れ替え

- フェーズ2.4:
  - tier2/tier3 装備のステータスを大幅強化し、ショップの装備抽選をレア度で重み付け
  - 「加護」タブを「持ち物」タブに改称（加護＋装備を小見出しで区切る）
  - 装備を1個ずつ数える在庫管理に修正（`equipItem` / `unequipItem` / `inventorySummary`）
  - ショップの装備・加護にセール（18%・20〜30%オフ）を追加
  - 静的ホスティング用の設定（`vercel.json` / `netlify.toml` / `docs/deploy.md`）を追加

- フェーズ2.5:
  - 盤面まわりの余白を圧縮（#app の padding 8→6・gap 6→4、盤面 max-height 96%、--board-min 160→140）し、
    浮いた分を下部バーへ（--bar-h-fixed 292→320px）
  - キャラカードの1行目を「名前だけ」に分離し、2行目へ ★・属性・役割・ID を移動（名前は「…」で切らない）
  - ★成長カーブ調整（starStatMul 0.95 / 2.1 / 5.0、starSkillMul 0.9 / 1.85 / 3.9）。
    ★1 をわずかに弱く、★2・★3 の伸びを大きくして、少数精鋭と多キャラの差を縮めた
  - 難易度引き上げ（chapterScale 0.5 / 2.3 / 3.8、章ボス専用の bossScale 1.35 を追加）。
    3章クリア率は 少数精鋭 12.5% / 多キャラ 11.5%
  - 敗北は通常戦も章ボスも同じ扱いにし、ノードを進めず同じ相手へ再挑戦（リザルトの［再挑戦］）
  - 救援コインを 2 → 4 に増額

- フェーズ2.6:
  - プレイヤーに見える文言を「ラン」→「挑戦」に統一（コードの識別子は run のまま）
  - 下部バーの「ラン」タブを「進行」に改名
  - タイトル画面を追加（`screenMode = 'title'`・下部バーなし）。「プラクティス」＝全キャラ／全装備／全加護を
    自由に試すサンドボックス、「挑戦」＝本編。保存データがあれば「続きから／最初から」を聞く
  - 「操作」「進行」タブから「挑戦を中断してタイトルへ」。オートセーブは保持され、プラクティスは
    セーブに一切書き込まない（テストで保証）
  - コイン表記を強調（ヘッダーの🪙チップ常時表示＋増減アニメ、購入・リロールボタンの価格をコイン付きで強調、
    買えない時はグレーアウトして無効化）

- フェーズ2.7:
  - 新しい挑戦のたびにシードを引き直すようにして、初期キャラのランダム化を修正
    （`createRun` は元からシード依存。UI が固定シード 'seed-1' を使っていたのが原因）
  - 盤面の上下の余白をゼロに（#app padding 6→4・gap 4→2・ヘッダー30→26px、
    盤面は `aspect-ratio` で高さを決め、余りは下部バーが吸う＝バーは最小320pxで伸びる）
  - ライフをハート列で表示（残り＝赤 #ff4757 の塗り／失った分＝灰色の輪郭）。
    ヘッダー・進行タブ・リザルト・タイトルで共通
  - 章別の通常戦を追加（C1B / C2A / C2B / C3A / C3B）。章が進むと顔ぶれと数が変わる
  - ショップのキャラ候補が尽きたら装備枠に振り替え（品数が減らない）。同じショップ内での
    キャラ重複も防止

- フェーズ2.8:
  - 敗北時の流れを「敗北 → 立て直しの特例ショップ（ShopOffer.retry）→ 再挑戦」に変更。
    通常戦も章ボスも同じ扱いで、ノードは進めない（`closeRetryShop` で閉じる）
  - メイン画面のサイズを固定（`--main-h` = 300px / `LAYOUT.mainHeight`）。screenMode でも
    画面サイズでも変わらず、余りは下部バーが吸う
  - 章ごとの章ボスを追加（B1 灰塵の巨像=燃焼／B2 氷霧の女王=凍傷／B3 蝕の王=麻痺＋HP50%・20%で変化）
  - PWA 対応（manifest.webmanifest・192/512 アイコン・theme-color・apple-touch-icon）。
    Service Worker は未対応
  - 難易度を再調整（chapterScale 0.7 / 3.6 / 6.2）。特例ショップのぶん易しくなった分を戻し、
    3章クリア率は 少数精鋭 8.5% / 多キャラ 8.5%

## 現在の作業

なし（フェーズ2.8 完了）

## 次にやること

- 描画を PixiJS に差し替える（`src/ui/replay.ts` のフレーム列をそのまま渡せる形にしてある）
- 章ごとの敵バリエーションを増やし、`EncounterDef.scale` を 1.0 に寄せる
- ショップ価格・イベント確率の本調整（今は全部仮の値）
- 公開URLへのデプロイ（`docs/deploy.md` の手順を実行する。CLI のログインだけ手作業が要る）

## 未解決の問題・決定事項

- 時間切れ（40秒からのサドンデス）の秒数と方式は暫定。火力と耐久のバランス調整後に見直す
- `EncounterDef.scale` は暫定。敵の種類が増えたら敵ごとの基本値で調整し、scale は 1.0 に寄せる
- （解決済み）チーム効果の割合指定に対応。`statMod` の `mode: 'pct'` と `filter` が使える
- シールドは持続時間なしの単一プール
- ラン進行の数値（価格・リロール・確率）はすべて仮。`src/game/config.ts` にまとめてある
- 章ごとの通常戦は既存の E1〜E4 を流用（`CHAPTER_BATTLES`）。章ボスは B1 のみ
