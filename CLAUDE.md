# Divine Pawns（仮題） — 神話オートバトラー・ローグライク

スマホの縦画面で遊ぶ、ブラウザ用のPvEオートバトラー×ローグライク。骨格はGuildrun、配置はTFT、加護とイベントはSlay the Spireを参考にする。既存作品の名称・キャラクター・アート・文言は流用しない。フリーゲームとして公開し、充実後に収益化も視野に入れる（素材は商用利用できるものだけを使う）。

要件定義書の全文は docs/requirements.md（存在する場合）。本書と食い違う時は要件定義書を正とする。

## 用語（この表記に統一する）

装備（「武器」ではない）、加護（「レリック」ではない）、コイン、ライフ、サポート枠、昇格、6体目枠。

## ゲーム構造

- 1回の「挑戦」（＝1プレイ通し）は3章（MVPは2章）。各章は「戦闘→ショップ→イベント」を2回繰り返し、章ボス、ボスショップ（1・2章のみ）と進む。休憩ノードとマップ分岐はない
- 戦闘のリザルト画面がそのままショップになる。勝っても負けても開く。品揃えはキャラ2・加護2・装備2で、リロールはコイン。キャラ候補は重複させず、候補が尽きたら（全員★3など）その枠は装備に振り替える。ボスショップは価格が約1.3倍で上位が出やすく、6体目枠か昇格が必ず1つ並ぶ
- イベントは二択。最初のイベントは両方安全（キャラ獲得かキャラ合成、タフネス系かパワー系の装備、加護かコイン）。2回目以降は「安全な支給か賭け支給」「安全なミニバトルか危険なミニバトル」
- 賭けで失うのは、ライフ1・装備1つ・加護1つ・所持コインの50%のいずれか。キャラは失わない。豪華報酬は高ティアの装備・強力な加護・6体目枠・昇格。結果はシードで決まり、開き直しても変わらない
- ライフは初期3で回復しない。敗北で-1、0でゲームオーバー。敗北は通常戦も章ボスも同じ扱いで、ノードは進めず同じ相手に再挑戦する（救援コイン+4）。安全なミニバトルの敗北は、ライフ減なし・報酬なし
- 開始はランダムな1体（挑戦ごとに新しいシードを引く。同じシードなら同じ初期キャラ）、ショップなし、初期コイン0。1章最初の戦闘は、どのキャラ単独でも勝てる強さにする
- コインは通常戦+5、章ボス+12、敗北時+4。売却は購入額の50%（加護は売却不可）

## 編成と枠

- 前衛枠3・サポート枠2の計5体。6体目枠で6体、昇格でサポート枠が前衛枠に変わる（最大3回。3回目は低確率だが理論上は可能）。ベンチはない
- サポート枠のキャラは盤面に出ず、サポート効果だけが働く。効果量は、装備を含む最終ステータスを参照する。サポート枠でも装備は有効
- 装備スロットの数は★の数と同じ（★1=1・★2=2・★3=3）。`equipmentSlots(star)` から求め、固定値では持たない
- 装備は1個ずつ数える。1個しか持っていない装備を2体に着けることはできない。着けると手持ちから1つ減り、外すと1つ戻る（`equipItem` / `unequipItem`）
- 装備のレア度（`tier`）が高いほど伸びが大きい。tier3 は1〜2個でキャラ1体ぶんに迫る
- 同じキャラは1体しか持たない。すでに持っているキャラをもう一度手に入れると、2体目にはならず★が1つ上がる（上限★3）。★3で所持中のキャラはショップ・報酬の候補から外れる
- ★が上がった瞬間に、まだ覚えていないスキルから3択で1つ覚える（選ばなかった2つは捨てる）。連続で上がった場合は待ち行列に積み、1件ずつ選び終わるまで先へ進めない
- キャラが最初から持つスキルは「アクティブ or パッシブ から1つ」＋「サポート1つ」の計2つだけ。残りは合成でしか増えない
- 昇格しなくてもクリアできるバランスにする

## 盤面と戦闘

- 六角グリッド。各陣営は3行（5・4・5列）で、敵陣も同じ形
- 0.1秒刻みの決定論シミュレーション。同じ入力とシードなら、ログまで完全に一致する。UIはログを再生するだけ
- 戦闘開始時は `Battle.resetAll()` で味方・敵とも定義どおりの初期値に戻す。敗北→再挑戦や同じノードへの再訪問で、HP・マナ・デバフ・バフ・クールダウン・位置を一切引き継がない
- ダメージ = 攻撃力 × 100 ÷ (100 + 防御)。ステータス計算は、基本値×★倍率 → 装備の固定値 → 割合補正の合計
- ターゲットは、最も近い敵 → 挑発 → 役割の優先度 → 残りHP → ID の順
- マナは攻撃で+10、被弾で+5。最大になるとアクティブスキルが自動で発動する
- 時間切れ: 40秒を超えると全員に、増え続けるダメージが入る。強制敗北はない。同時全滅は敗北（設定値）。この仕様は、火力と耐久のバランス調整後に見直す（保留）
- 系統特性・配置特性・属性相性のような、全体共通の有利不利の仕組みは設けない。同様の効果は、キャラ・装備・加護がそれぞれ固有の効果として持つ
- 効果は「きっかけ・条件・効果」のデータで表し、効果量は「参照ステータス × 係数」で書く
- 加護はチーム効果として扱い、ユニットにしない。効果量は固定値でも割合（参照ステータス×係数）でも書ける

## 属性デバフ（上限なし）

- 属性は炎・氷・木・雷。属性はシナジー条件のタグで、その属性のデバフスキルを覚えやすい（全員ではない）
- 燃焼: 毎秒、ストック×係数のダメージ。ストックは毎秒1減る
- 凍傷: 攻撃速度 ÷ (1 + 0.05 × 有効ストック)。ストックは毎秒1減る
- 猛毒: 付与で効果時間が延び、5秒ごとにダメージが増える
- 麻痺: 阻止確率 = 有効ストック ÷ (有効ストック + 20)。阻止するとスキルは不発、マナは半分に戻り、ストックは0になる
- ストック・効果時間・秒間ダメージに上限はない。ストック1あたりの効果は小さくし、敵とボスの基礎値を高くして釣り合わせる。狙いは「スケーリングの快感」
- 燃焼と猛毒は防御無視で耐性なし。凍傷と麻痺は、有効ストック = ストック × (1 − 耐性)。耐性は通常敵0〜20%、ボス40〜60%

## 世界観

- 神話の「意外とマイナーな神・英雄」がプレイアブル。「有名な神・英雄」は加護として力を貸す
- 初期は4神話（ギリシャ・北欧・日本・エジプト）×キャラ2体の8体と、加護の神8柱×2種の16種。将来の追加候補は、シュメール・クトゥルフ・インド・アステカ・ゾロアスター
- 現存する信仰に関わる神話は敬意を保って描く。クトゥルフは原典の要素だけを使う

## 数値とUI

- 数値は序盤に小さく、終盤に大きく伸びてよい。表示は 1.2K / 3.4M / 5.6B / 7.8T、1e15以上は指数表記。コインは2〜3桁に収める
- スマホの縦画面（幅360〜430px）が基準。本文16px以上、補足14px以上、タップ領域44px以上
- 説明文は簡潔にし、倍率は書かない。装備やスキルの説明は、ボタンで開くポップアップにまとめる
- イベントの選択肢には、選ぶ前に「〈安全〉〈賭け〉〈ミニ戦闘〉＋何が得られる／失われるか」を添える。選んだあとは結果を1画面で見せてから次へ進む
- リザルトとノード進行は1画面にまとめる（大きな結果テキスト＋小さめの「次へ」）。進行マップの俯瞰は「進行」タブの役割
- 挑戦中に見えるのは所持しているものだけ（キャラ・加護・装備）。装備は同じものをまとめて「×2」と出す
- 挑戦中に★を直接変える操作は出さない（同キャラ再獲得だけ）。★セレクタはプラクティスでのみ出す
- 配置は「編成タブに出ているキャラ」を盤面のマスにタップで置く。空きマスなら移動、他の味方のマスなら入れ替え（どちらも1タップ）
- 装備はポップアップ（シートスタック）で開く。カード上に展開するトグルは持たない
- 盤面アイコンには shortName（仮名の最初の「の」まで）を表示し、一覧や詳細パネルには正式名とIDを別々に表示する
- 画面は「上＝メイン画面」「下＝タブ付きの操作バー（編成／持ち物／進行／操作）」の2層
- 起動時はタイトル画面（`screenMode = 'title'`、下部バーなし）。「プラクティス」＝全キャラ・全装備・全加護を自由に試すサンドボックス、「挑戦」＝本編。プラクティスは挑戦のセーブに一切触れない
- 「操作」タブと「進行」タブから、いつでも「挑戦を中断してタイトルへ」戻れる（オートセーブは残る）
- ライフと所持コインはヘッダーに常時表示。ライフは残り＝赤の塗り（`--life` #ff4757）／失った分＝灰色の輪郭のハートで、ヘッダー・進行タブ・リザルト・タイトルで同じ見た目にする
- 所持コインはヘッダーに常時表示（🪙アイコン＋金色のチップ、増減時は差分と点滅）。ショップの購入・リロールは価格をコイン付きで出し、買えない時はグレーアウトして押せない
- 「持ち物」タブでは加護と装備を小見出しで区切って見せる。装備は「所持 n／装備中 m／空き k」を出す
- メイン画面は `screenMode`（title / prep / battle / result / shop / event）で切り替える。盤面を出すのは prep と battle だけで、リザルト・ショップ・イベントはメイン画面に描く
- タブ操作を止めるのは戦闘の再生中（battle）だけ。ショップ・イベント・リザルト中も編成や加護を確認できる
- 下部バーは最小 320px（`--bar-h-fixed`）で、画面の余った高さを吸って伸びる。タブによって高さは変わらない。中身が多いタブは内部スクロールで吸収する
- 盤面の入れ物は伸び縮みせず（`flex: 0 1 auto`）、盤面は `aspect-ratio` で高さが決まる。盤面の上下に余白は出さない（`tests/layout.test.ts` が 375×667 / 390×844 / 428×926 で 0 を確認する）
- キャラカードは「名前／★・属性・役割・ID／状態＋簡易ステータス／操作」の4行。縦の予算は `src/ui/layout.ts`、`tests/layout.test.ts` が CSS と突き合わせる
- 「編成」タブはキャラを1体ずつカード表示し、◀▶ で送る。表示中のキャラの居場所は盤面をピンク（#ff7ae0）でハイライトする
- カードの1行目は名前だけ（「…」で切らず、長い名前は1段小さくする）。2行目に ★・属性・役割・ID を並べる
- ショップの装備・加護は確率でセールになる。「SALE」バッジと「n% オフ（定価 mc）」を添える

## 開発ルール

- 戦闘エンジンはUIに依存しない純粋なTypeScript。乱数はシード付きで、「戦闘」「ショップ」「イベント」の系列を分ける
- キャラ・敵・装備・加護はデータで定義し、zodで検証する。仮の名前には「（仮）」を付ける
- 変更したら必ず `npm run test` と `npm run sim:determinism` を通す。戦闘ロジックを変えたら `npm run sim:all` も通す
- 遭遇ごとの強さ補正（EncounterDef.scale）は暫定。敵の種類が増えたら、敵ごとの基本値で調整する
- 挑戦全体の難易度カーブは「章ごとの遭遇（顔ぶれ）」＋ `RunConfig.chapterScale`（章ごとの敵の強さ倍率）＋ `RunConfig.bossScale`（章ボスへの上乗せ）で作る。通常戦は1章 E1・C1B／2章 C2A・C2B／3章 C3A・C3B。`EncounterDef.scale` は配置検証（sim:placement）の基準
- 3章クリア率の目標は約10%（`sim:balance` の少数精鋭／多キャラ両ビルドで 5〜20%）
- 公開は静的ホスティング（`vercel.json` / `netlify.toml` を同梱）。手順は `docs/deploy.md`
- バランスの目標は「少数精鋭」と「多キャラ」のどちらでもクリアを狙えること。`sim:balance` が両ビルドの通しクリア率を出す
- 確認を求めずに最後まで進める。決まっていない細部は仮決めして、最終報告に列挙する。成功時は「✅ 完了。」と数値、失敗時は「❌」とログを返す

## 進行状況

- フェーズ1（戦闘エンジンとシミュレーション）: 完了
- フェーズ1.5（UI整備・技術負債の解消・CLAUDE.md）: 完了
- フェーズ2（UI仕上げ＋ラン進行の骨格）: 完了
- フェーズ2.1（戦闘リセット・初戦難易度・カード縦圧縮・メイン画面再編）: 完了
- フェーズ2.2（装備・加護の拡充、スキル選択制、リロール段階価格、UI/UX仕上げ）: 完了
- フェーズ2.3（同キャラ重複廃止・タップ配置・バー縮小・ビルド多様性）: 完了
- フェーズ2.4（レア装備強化・持ち物タブ・装備の在庫管理・セール・公開配信）: 完了
- フェーズ2.5（盤面圧縮とカードの名前行分離・★成長カーブ・難易度引き上げ・敗北＝再挑戦）: 完了
- フェーズ2.6（「挑戦」への名称統一・タイトル画面とプラクティス・コイン表記の強調）: 完了
- フェーズ2.7（初期キャラのランダム化・盤面余白ゼロ・ライフ表示・章別の敵・★3時の代替報酬）: 完了
- 次: フェーズ3（PixiJS への描画差し替え、敵・ボスの追加、ショップ価格の本調整）

## リポジトリ情報

### npm スクリプト

| コマンド | 用途 |
| --- | --- |
| `npm run dev` | 検証用画面を起動（Vite・http://localhost:5173） |
| `npm run build` | 型チェック＋本番ビルド（dist/） |
| `npm run preview` | ビルド結果の確認 |
| `npm run typecheck` | 型チェックのみ |
| `npm test` | Vitest を1回実行 |
| `npm run test:watch` | Vitest の監視実行 |
| `npm run sim:determinism` | ランダム戦闘1,000件を2回ずつ実行し、ログのハッシュを比較 |
| `npm run sim:first-battle` | 8体それぞれ単独で、味方14マス全てから E0 に挑む |
| `npm run sim:placement` | E1〜E4・B1 で配置を総当たりし、勝てる配置の割合を見る |
| `npm run sim:balance` | ランダム編成1,000件 × 全遭遇。勝率・戦闘時間・属性別DPS |
| `npm run sim:scaling` | 基準編成と燃焼ビルドの秒間ダメージ倍率 |
| `npm run sim:perf` | 戦闘10,000件の所要時間 |
| `npm run sim:all` | 上の全部を実行し、reports/ に書き出す |

### ディレクトリ

| パス | 中身 |
| --- | --- |
| `src/engine/types.ts` | 共通型（効果システム・データ定義の形） |
| `src/engine/config.ts` | 設定値をすべて集約（`DEFAULT_CONFIG`） |
| `src/engine/hex.ts` | 六角グリッド（odd-r）・距離・使えるマス・BFS |
| `src/engine/rng.ts` | sfc32 のシード付き乱数（戦闘／ショップ／イベントの3系列） |
| `src/engine/stats.ts` | ステータス計算・ダメージ式・役割ごとの基本値 |
| `src/engine/debuffs.ts` | 燃焼・凍傷・猛毒・麻痺の数式と状態 |
| `src/engine/unit.ts` | 戦闘中のユニット |
| `src/engine/battle.ts` | 戦闘ループ・効果の実行・チーム効果 |
| `src/engine/build.ts` | 編成＋遭遇 → 戦闘セットアップ |
| `src/engine/log.ts` | イベントログとハッシュ |
| `src/data/` | キャラ・敵・遭遇・装備・加護（zod で検証） |
| `src/game/` | 挑戦の進行（章のステートマシン・ショップ・イベント・ライフ・オートセーブ）。コード上の識別子は run のまま |
| `src/ui/` | 画面（DOM + SVG）。`replay.ts` がログをフレーム列に、`layout.ts` が縦の予算を持つ |
| `src/util/format.ts` | 数値の短縮表記 |
| `sim/` | シミュレーション用コマンド |
| `reports/` | シミュレーション結果（`phase1-report.md` と JSON） |
| `tests/` | Vitest |
| `docs/` | 要件定義書などのドキュメント |

### 設定値の場所

すべて `src/engine/config.ts` の `DEFAULT_CONFIG`。個別に差し替えるときは `cloneConfig()` を使う。

- 時間切れ: `timeout.startSeconds`（40秒）／`basePct`（1%）／`stepPct`（1%）、`maxDuration`（90秒）
- マナ増加量: `manaOnAttack`（10）／`manaOnHit`（5）
- ★倍率: `starStatMul`（★1=0.95／★2=2.1／★3=5.0。HP・攻撃力）／`starSkillMul`（★1=0.9／★2=1.85／★3=3.9）
- 攻撃の初期遅延: `initialAttackDelay`（既定0。個体ごとに `BattleUnitSpec.initialAttackDelay` で上書き）
- 同時全滅の扱い: `simultaneousWipe`（既定 `'lose'`。`'win'` `'draw'` も選べる）
- デバフ係数: `debuff.burn` / `frostbite` / `poison` / `paralysis`
- 固定タイムステップ: `tick`（0.1秒）／防御定数: `defenseConstant`（100）／枠数: `team`／ティア倍率: `tierMul`

UI の文字サイズとタップ領域は `src/ui/style.css` の `:root`（`--font-body` `--font-sub` `--font-heading` `--tap` など）。

挑戦の進行の設定値は `src/game/config.ts` の `DEFAULT_RUN_CONFIG`（章数・ライフ・コイン・ショップ価格・リロール・枠拡張）。すべて仮のバランス値。

### データの追加手順

| 種類 | ファイル |
| --- | --- |
| キャラ | `src/data/characters.ts`（`skills` がプール・`initialSkills` が初期2つ） |
| 敵・ボス | `src/data/enemies.ts` |
| 遭遇 | `src/data/encounters.ts` |
| 装備 | `src/data/equipment.ts`（`tier` はレア度・`element` は属性特化） |
| 加護 | `src/data/blessings.ts`（`desc`＝1行の説明・`summary`＝説明ポップアップ・`rarity`） |
| イベント | `src/data/events.ts`（`safeOnly` が章の最初のイベント用） |
| zod スキーマ | `src/data/schema.ts` |
| shortName の決め方 | `src/data/naming.ts` |

1. 該当ファイルの配列に1件足す。`name` には「（仮）」を付ける
2. `shortName` は `deriveShortName(name)` の結果と同じにする（最初の「の」まで／なければ先頭3文字／最大4文字）
3. すべての効果に `summary`（倍率を書かない短い説明文）を付ける
4. ステータスは `makeBase(role, tier)` を土台にし、必要な項目だけ上書きする
5. `npm test`（zod 検証と命名規則のテストが走る）→ `npm run sim:determinism` → 戦闘ロジックを触ったなら `npm run sim:all`

### フェーズ1で仮決めした値

- 役割ごとの基本ステータス（`ROLE_BASE`）とティア倍率（1.0 / 1.1 / 1.2）。ティアは基本値の定義時に掛け、戦闘中の計算順には混ぜない
- 敵の基本値は「通常戦の標準」に合わせ、遭遇ごとの強弱は `EncounterDef.scale` で調整（E1=足慣らし・迷い火×1.2＋兵士×0.28、E2=×1.2、E3=×1.02/0.92、E4=×0.95〜1.0、B1=ボス×1.0＋取り巻き×0.56）
- E1 は1章1戦目。所持キャラ1体でも必ず勝てる強さにしてあり、`sim:first-battle` が E0 と E1 の両方を8体×14マスで検証する
- 「隣接する味方1体につき+X%」は、`adjacentAllies min:1〜6` の条件つき効果6段で表現する
- スキルのダメージも通常ダメージと同じ防御式を通す。燃焼・猛毒だけ防御無視
- 燃焼まわりの補正（係数+20%／ダメージ+10%／業火の加護×2）は、付与時点で係数に畳み込んで確定させる
- 燃焼・猛毒の継続ダメージは付与者に帰属させ、属性別DPSでは燃焼=炎・猛毒=木として数える
- シールドは持続時間なしの単一プール（`duration` は未使用）
- スキル使用と通常攻撃の間隔は独立（同じティックに両方起こりうる）
- 暗殺型は「最後列を優先 → 距離 → 挑発 → 役割 → 残HP → ID」の順で狙う
- `sim:scaling` の秒間ダメージは、反撃しない「計測用の的」に対する 0〜30秒（サドンデス前）の窓で測る
- `LoadoutEntry.skills` を省略した場合は「初期スキル ＋ プール順で (★-1) 個」を既定とする（シミュレーション用）
- ショップのリロールは1回目1c・2回目2c…と増え、ショップに入り直すと1cに戻る
- 装備の価格はレア度ごと（★1=3c・★2=5c・★3=9c）。出現重みは通常 55/33/12、ボスショップ 15/45/40
- セールは装備と加護だけ。1件ごとに18%の確率で、20〜30%オフになる
- 画面はログに加えて各ティックのスナップショット（HP/マナ/デバフ/位置/実効ステータス）を再生する
- 加護のレア度は `common` / `rare` / `epic` の3段階（表示は コモン／レア／エピック）
- ポップアップは重ねられるスタック方式（`state.sheets` の配列）。すべての階層を同時に描き、最前面だけ操作できる（背面は `pointer-events: none`）。背景タップ・×・Escape で1つ上の階層に戻る
- 戦闘の再生中は「編成」「持ち物」「進行」タブを無効化し、「操作」タブだけ使える（倍速とスキップは常に操作可）
- プレイヤーに見える文言では「ラン」と書かず「挑戦」と書く（コード側の `RunState` などの識別子は run のまま）
